{
  "name": "sudoku-activity",
  "version": "1.0.0",
  "scripts": {
    "dev:server": "node server/index.js",
    "dev:client": "vite client",
    "build": "vite build client --outDir ../server/dist",
    "start": "npm run build && node server/index.js"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "socket.io": "^4.7.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
